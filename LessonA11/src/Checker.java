/**
 * 
 * @author Michelle Li
 * Period 1
 *
 */

public interface Checker {

	/**
	 * Method accept to determine if checker accepts text or not
	 * @param text - a string to consider for acceptance
	 * @return - true if checker accepts text, false otherwise
	 */
	boolean accept(String text);
	
}
